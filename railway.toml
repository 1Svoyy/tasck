[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "npx prisma migrate deploy 2>&1; echo '=== NODE START ==='; node dist/index.js 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
