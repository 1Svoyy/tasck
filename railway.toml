[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run db:generate && npm run build"

[deploy]
startCommand = "npm run db:migrate && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[nixpacks]
pkgs = ["nodejs_20", "openssl"]
